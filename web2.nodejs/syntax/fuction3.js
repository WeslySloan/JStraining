//Math.round(A,B) = A.B값 반올림하는 함수

console.log(Math.round(1.6)); //2 
console.log(Math.round(1.4)); //1
 
function sum(first, second){ // parameter 매개 변수 = (first, second) = 함수지정용 변수
  console.log(first+second);
}
 
sum(2,4); // argument 인자 = (2.4) = 함수입력내용물